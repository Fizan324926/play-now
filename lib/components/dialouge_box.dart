import 'package:flutter/material.dart';

class Dialouge extends StatelessWidget {
  const Dialouge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Padding(
        padding: EdgeInsets.all(20),
      ),
    );
  }
}
